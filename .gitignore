# Large source data — NOT needed in the repo (precompute runs locally)
hybas/
_cache/
IDF/
*.pdf

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Pipeline runtime that is regenerated / cached (git has no inline comments,
# so these patterns must sit on their own lines)
operational/out/
# rolling store — persisted between CI runs via the Actions cache
operational/state/frames_L*.csv
# regenerated every cycle by the workflow (deployed via Pages artifact)
operational/web/data/alerts_L*.json
operational/web/data/pins.geojson

# Committed precompute artifacts (DO keep these):
#   operational/state/attrs_L*.csv
#   operational/state/pixel_cat_L*.npz
#   operational/web/data/catchments_L*.geojson
#   operational/web/data/levels.json
